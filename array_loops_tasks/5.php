<?php

/*5. Дан массив $arr с ключами 'Коля', 'Вася', 'Петя' с элементами '200', '300', '400'. С помощью
цикла foreach выведите на экран столбец строк такого формата: 'Коля — зарплата 200
долларов.'.*/

$arr = array('Коля' => '200', 'Вася' => '300', 'Петя' => '400');
foreach ($arr as $name => $dollar) {
    echo "$name - зарплата $dollar долларов. <br> \n";
}


























































 /* $array = range(1,3000,6);
  $foo = true;

  

  
  while ($foo)){
      $thisarray =
	  if($thisarray > 140 && $thisarray < 150){
	      echo $thisarray;
	  }
	  die('finish');
 }
 */

 /* $array = range(1,3000,6);
  $foo = true;




  do {
      $thisarray =
       if ($thisarray > 140 && $thisarray < 150) {
           echo $thisarray;
           break;
       }
      }  while ($foo) ;

  echo 'test';
  */
 /*$array = range(1,3000);


 foreach(($array as $value) && ($key => $value) {

     if(($a = $value % 2) && ($a > 0)){
         echo $a. "<br>";

     }
     else {

       echo 'Было использовано continue';
         continue;

     }

 }
*/


 

  
